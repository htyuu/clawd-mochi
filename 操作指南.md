<!-- LOGO -->
<p align="center">
  <img src="pics/clawd_mochi_banner.png" alt="Clawd Mochi" width="700"/>
</p>

# Clawd Mochi 完整操作指南 🦀

从零开始，把一堆零件变成会读懂 Claude Code 心情的桌面小伙伴。

**总时长**：硬件 ~1 小时 + 软件 ~30 分钟
**总成本**：~$6-8
**难度**：新手友好（每一步都有截图/示意/排错）

---

## 📚 目录

| 章节 | 内容 |
|------|------|
| [一、选版本](#一选版本) | 原版 vs Companion 版选哪个 |
| [二、购买零件](#二购买零件) | 清单 + 哪里买 |
| [三、3D 打印外壳](#三3d-打印外壳) | 打印参数 |
| [四、硬件接线](#四硬件接线) | 8 根线怎么接 |
| [五、配置 Arduino IDE](#五配置-arduino-ide) | 装环境 + 装库 |
| [六、烧录固件](#六烧录固件) | 上传程序 |
| [七、配置 WiFi](#七配置-wifi) | 仅 Companion 版 |
| [八、安装 Python Daemon](#八安装-python-daemon) | 仅 Companion 版 |
| [九、组装外壳](#九组装外壳) | 收尾 |
| [十、验证与使用](#十验证与使用) | 测试一切是否正常 |
| [十一、常见问题](#十一常见问题) | 排错 |
| [十二、二次开发](#十二二次开发) | 改造你的玩偶 |

---

## 一、选版本

本仓库提供 **两套独立的固件**，你只能烧一个，但随时可以重烧切换。

| | **原版** (`clawd_mochi`) | **Companion 版** (`clawd_mochi_companion`) ⭐ |
|---|---|---|
| 功能 | 像素眼睛、画板、终端、Logo 动画 | 原版功能 + 实时显示 Claude 工作状态 |
| 控制 | 手机连热点直控 | 手机 + 电脑 Daemon 双通道 |
| WiFi | 只开 AP 热点 | AP + STA 双模，能连家里 WiFi |
| 多 WiFi | ❌ | ✅ 最多存 5 个，自动切换 |
| 心情灯 | ❌ | ✅ 屏幕颜色随 Claude 状态变 |
| Token 进度条 | ❌ | ✅ |
| 每日统计 | ❌ | ✅ SQLite 持久化 |
| 安装难度 | ⭐ | ⭐⭐ 多一步装 Daemon |
| 适合 | 只想要个会眨眼的摆件 | 重度使用 Claude Code 的开发者 |

> 💡 **建议**：先烧原版看看效果，确认硬件没问题再烧 Companion 版。这份文档主要按 Companion 版来写，原版需要跳过第 7、8 节。

---

## 二、购买零件

| 零件 | 规格 | 数量 | 参考价格 | 备注 |
|------|------|------|----------|------|
| **ESP32-C3 Super Mini** | 带 WiFi 的微控制器 | 1 | ~¥15-20 | 淘宝/拼多多搜"ESP32-C3 Super Mini" |
| **ST7789 TFT 屏幕** | 1.54寸, 240×240, SPI 接口, **7 针版** | 1 | ~¥20 | 一定要是 SPI 接口和 240×240 分辨率 |
| **杜邦线** | 母对母, 10cm | 8根 | ~¥3 | 一把 40 根，剩下的以后玩别的项目 |
| **M2×6mm 螺丝** | 自攻螺丝 | 2颗 | 几毛 | 固定屏幕用 |
| **双面胶** | 任意 | 一小片 | 几毛 | 固定 ESP32 |
| **USB-C 数据线** | 必须能传数据 | 1根 | — | 注意不是只能充电的线！ |
| **3D 打印外壳** | PLA 或 PETG, ~30g | 1套 | ~¥5（找代打印） | 自己有打印机就免费 |

**总成本：~¥45-55**

### 💡 购买注意

1. **ESP32-C3 Super Mini** ≠ ESP32 / ESP8266 / ESP32-S3，规格不同，引脚定义也不一样
2. **ST7789** 屏幕一定要选 **SPI 接口、240×240、1.54寸、7 针**（VCC/GND/SCL/SDA/RES/DC/CS/BL，注意 7 针的话 BL 可能要单独接）
3. **USB-C 线**：买"数据线"，不要图便宜买"快充充电线"，很多充电线只有 2 根电源线，没有数据线，电脑识别不到 ESP32

---

## 三、3D 打印外壳

仓库提供了 3 套模型，**Companion 版用第一套**（带屏幕开孔的电子外壳）：

```
models/
├── clawd_mochi/                      ← ⭐ 这个！带屏幕窗口和 USB 槽
│   ├── clawd_mochi_v1.stl
│   └── clawd_mochi_v1.3mf
├── clawd_3d/                         ← 纯装饰摆件（不带电子）
└── clawd_3d_squished_eyes/           ← 装饰摆件，眯眼版
```

### 打印参数

| 参数 | 值 |
|------|-----|
| 材料 | PLA 或 PETG |
| 层高 | 0.15–0.20 mm |
| 填充 | 15% 陀螺填充（gyroid） |
| 支撑 | 需要（屏幕窗口悬空处） |
| 方向 | 正面朝下，背板朝上贴板 |
| 推荐颜色 | 身体橙色（#DA1100）+ 背板哑光黑 |

### 自己没有打印机？

- **淘宝代打印**：找"3D 打印代打印"店家，发 STL 文件，~¥5-10
- **MakerWorld 直接下单**：https://makerworld.com/zh/models/2559505

---

## 四、硬件接线

> ⚠️ **接线前断电！** ESP32 别插 USB，免得短路烧板。

### 接线表

| ST7789 引脚 | ESP32-C3 引脚 | 建议线色 | 作用 |
|------------|--------------|---------|------|
| VCC | **3V3**（绝对不能接 5V！） | 🔴 红 | 电源 |
| GND | GND | ⚫ 黑 | 地线 |
| SDA | GPIO 10 (MOSI) | 🟠 橙 | SPI 数据 |
| SCL | GPIO 8 (SCK) | 🟢 绿 | SPI 时钟 |
| RES | GPIO 2 | 🟣 紫 | 屏幕复位 |
| DC | GPIO 1 | 🔵 蓝 | 数据/命令切换 |
| CS | GPIO 4 | ⚪ 白 | 片选 |
| BL | GPIO 3 | 🟡 黄 | 背光 |

### 接线步骤

```
1. 把 8 根母对母杜邦线一头分别按颜色对照接到屏幕背面排针上
2. 另一头接到 ESP32 对应 GPIO（板子上有丝印标号）
3. 接 VCC 到 3V3 时停下来再确认一次——这是唯一会翻车的点
4. 用手轻轻拉每根线，确认插紧了不松
5. 暂时不要插 USB
```

### 🔥 接错会怎样？

| 错误 | 后果 |
|------|------|
| VCC 接 5V | 屏幕直接烧毁 💀 |
| GND 没接 | 屏幕不亮或闪烁 |
| SDA / SCL 接反 | 屏幕白屏或黑屏 |
| 杜邦线没插紧 | 时灵时不灵 |

---

## 五、配置 Arduino IDE

### Step 1：下载安装 Arduino IDE 2.x

去 https://www.arduino.cc/en/software 下载 **Arduino IDE 2.x**（不是老版 1.x），装好打开。

### Step 2：添加 ESP32 开发板支持

Arduino 默认不认 ESP32，需要手动加：

1. 菜单栏 → **File → Preferences**（Mac 是 **Arduino IDE → Settings**）
2. 找到 **Additional boards manager URLs** 那一栏
3. 粘贴这个地址：

```
https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
```

> 💡 国内下载慢可以用清华镜像：
> ```
> https://mirrors.tuna.tsinghua.edu.cn/github-raw/espressif/arduino-esp32/gh-pages/package_esp32_index.json
> ```

4. 点 **OK** → **Tools → Board → Boards Manager** → 搜索 `esp32` → 找到 **"esp32 by Espressif Systems"** → **Install**

### Step 3：安装必要的库

菜单栏 → **Tools → Library Manager**，搜索并安装：

| 库名 | 用途 | 原版需要 | Companion 需要 |
|------|------|----------|----------------|
| **Adafruit GFX Library** | 通用绘图 | ✅ | ✅ |
| **Adafruit ST7735 and ST7789 Library** | 屏幕驱动 | ✅ | ✅ |
| **ArduinoJson** by Benoit Blanchon | JSON 解析 | ❌ | ✅ |

> ⚠️ Companion 版**必须**额外装 ArduinoJson，否则编译会失败。

### Step 4：配置开发板参数

用 USB-C 连接 ESP32-C3 到电脑，然后在 **Tools** 菜单逐项设置：

| 设置项 | 选择 |
|--------|------|
| Board | **ESP32C3 Dev Module** |
| **USB CDC On Boot** | **Enabled** ⚠️ 必须开！ |
| CPU Frequency | 160 MHz |
| Flash Size | 4MB (32Mb) |
| Partition Scheme | Default 4MB with spiffs |
| Upload Speed | 921600 |
| Port | 选你 ESP32 对应的串口（见下） |

#### 怎么选 Port？

| 系统 | 看起来像 |
|------|----------|
| Mac | `/dev/cu.usbmodem...` 或 `/dev/cu.usbserial-...` |
| Windows | `COM3`、`COM5`...（数字不一定） |

**确认方法**：先看下 Port 列表，再插 USB-C，重新看，**新冒出来的那个就是 ESP32**。

#### Port 是灰色的怎么办？

| 原因 | 解决 |
|------|------|
| USB 线只能充电 | 换数据线 |
| USB 口接触不良 | 换个口 |
| 板子没识别 | 拔了重插 |
| 驱动没装 | ESP32-C3 一般免驱，少数旧 Mac 需要装 CH340 驱动 |

---

## 六、烧录固件

### 选择固件

```bash
# 原版（纯摆件）
clawd_mochi/clawd_mochi.ino

# Companion 版（Claude Code 桌面伙伴）⭐
clawd_mochi_companion/clawd_mochi_companion.ino
```

### 烧录步骤

1. **File → Open** → 选上面对应的 `.ino` 文件
2. 确认 Tools 菜单的开发板设置全部正确
3. 点工具栏 **→ Upload**（或快捷键 `Cmd+U` / `Ctrl+U`）
4. 等待编译 + 上传（首次比较慢，~1 分钟）
5. 看到这行表示成功：

```
Hard resetting via RTS pin...
```

### 烧录失败怎么办？

| 错误 | 解决 |
|------|------|
| `Failed to connect` | 按住板子上的 **BOOT** 键不放 → 点 Upload → 看到 "Connecting..." 后松开 |
| `Wrong boot mode` | 同上 |
| 编译报错 `Adafruit_GFX.h: No such file` | 库没装好，回 Step 3 |
| 编译报错 `ArduinoJson.h: No such file` | Companion 版漏装 ArduinoJson |
| 烧录成功但屏幕黑屏 | 检查接线，重点查 VCC 是不是 3.3V、GND 是不是接好了 |

### 烧录成功的标志

屏幕依次显示：

```
1. "Clawd Mochi" 启动画面（~1.2 秒）
2. Anthropic 蟹爪 Logo 逐帧绘制动画
3. WiFi 信息屏：
   ┌──────────────────────────┐
   │ WiFi: ClaWD-Mochi        │
   │ password: clawd1234      │
   │ AP IP: 192.168.4.1       │
   │ STA: setup STA at /wifi  │  ← Companion 版才有这行
   └──────────────────────────┘
```

> 看到这个屏幕就说明硬件 + 烧录都成功了 ✅

---

## 七、配置 WiFi

> ⏭️ 烧录的是原版？跳到[第九节](#九组装外壳)。

Companion 版需要让 ESP32 连上你家 WiFi，才能接收电脑推送的 Claude 状态。

### Step 1：首次连接 ESP32 热点

1. 手机 / 电脑搜索 WiFi
2. 连接 `ClaWD-Mochi`，密码 `clawd1234`
3. 浏览器打开 http://192.168.4.1

会看到 Web 控制器主页。

### Step 2：进入 WiFi 配置

主页底部点击 **📡 WiFi setup & networks**，跳转到 `/wifi` 配置页。

页面长这样：

```
┌─────────────────────────────────┐
│  🦀 Clawd Mochi · WiFi          │
├─────────────────────────────────┤
│  Connected: <未连接>             │
├─────────────────────────────────┤
│  Saved networks                 │
│  （目前是空的）                  │
├─────────────────────────────────┤
│  Nearby networks                │
│  [🔄 Scan]                      │
├─────────────────────────────────┤
│  Add manually                   │
│  [SSID________________]         │
│  [Password____________]         │
│  [Save]                         │
└─────────────────────────────────┘
```

### Step 3：添加你家 WiFi

**方式 A：扫描列表选**（推荐）

1. 点 **🔄 Scan**，列出周围所有 WiFi（按信号强度排序）
2. 找到你家 WiFi，点旁边的 **use** 按钮
3. 输密码 → **Save**

**方式 B：手动输入**

直接在底部输入框填 SSID 和密码 → **Save**

### Step 4：验证连接

保存后 ESP32 自动尝试连接（~5 秒），成功后页面顶部会显示：

```
Connected: home-wifi (192.168.1.123, -45dBm)
```

**记住这个 IP 地址 `192.168.1.123`**，下一节会用到。

> 💡 ESP32 屏幕重启后也会显示这个 IP。

#### IP 地址怎么找？

如果你忘了 IP，有三种方式查到：

| 方式 | 操作 |
|------|------|
| **ESP32 屏幕** | 重启 ESP32，启动画面最后一屏会显示 `STA: <WiFi名>` 和 `IP: <IP>` |
| **路由器后台** | 登录路由器管理页面，找已连接设备列表里的 `ClaWD-Mochi` 或 `ESP32` |
| **Web 控制器** | 手机先连 ESP32 热点 `ClaWD-Mochi`，打开 http://192.168.4.1 → 页面底部或 `/state` 接口可看到 STA IP |

### Step 5（可选）：添加更多 WiFi

最多可以存 5 个 WiFi（如：家、公司、手机热点）。带玩偶去哪都自动连最强信号。

### 验证：电脑和 ESP32 能互相通信吗？

> 这一步在下一节（安装 Daemon）之前一定要做，否则 daemon 推了东西 ESP32 也收不到。

确保**你的电脑连的是同一个家里 WiFi**，然后：

```bash
# 从电脑 ping 一下 ESP32（IP 换成上面记的那个）
ping 192.168.1.123
```

如果看到类似：

```
64 bytes from 192.168.1.123: icmp_seq=0 ttl=255 time=5.234 ms
```

说明通 ✅

如果看到 `Request timeout` 或 `100.0% packet loss`：

- 电脑是不是没连你家 WiFi？（可能还连着 ESP32 的热点 `ClaWD-Mochi`）
- ESP32 是不是没通电？
- 屏幕有没有显示 `STA: <你家WiFi名>` 和 IP？

### 连不上 WiFi 的替代方案

如果手边没有 2.4GHz WiFi（比如公司网络只有 5GHz），ESP32 连接失败后会**退回到仅 AP 模式**。这时候：

- 电脑连 `ClaWD-Mochi` 热点
- 浏览器中访问 http://192.168.4.1 的 Web 控制页面仍然可用
- 但 **Daemon 推送需要家里 WiFi**，所以在电脑和 ESP32 连同一个局域网之前，Status 视图不会收到数据

---

## 八、安装 Python Daemon

> ⏭️ 烧录的是原版？跳到[第九节](#九组装外壳)。

> ⚠️ **顺序提醒：在做这一节之前，必须先做完第 6 节（烧录固件）和第 7 节（配置 WiFi）**。
> 因为你需要 ESP32 在家里 WiFi 下的 IP 地址才能配置 daemon。

Daemon 是一个跑在你电脑上的小程序，作用是：

```
Claude Code (你电脑) ──hooks──→ Daemon ──HTTP──→ ESP32 (家里WiFi)
```

### 前置条件

- macOS 或 Linux（Windows 没测试过，但理论可以）
- Python 3.10+
- 推荐用 conda 管理环境

### Step 1：安装 conda（已装跳过）

如果还没装 conda，用 Homebrew 装 Miniconda（macOS）：

```bash
# 先设置国内镜像加速
export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.ustc.edu.cn/homebrew-bottles
export HOMEBREW_API_DOMAIN=https://mirrors.ustc.edu.cn/homebrew-bottles/api

brew install --cask miniconda

# 让 shell 认识 conda 命令
conda init zsh   # 或 conda init bash
```

**关掉终端，重新打开一个新的**。

### Step 2：创建 conda 环境

```bash
# 接受 conda 频道条款（首次需要）
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r

# 创建环境
conda create -n clawd python=3.13 -y

# 激活
conda activate clawd
```

激活成功后命令行前面会出现 `(clawd)` 提示符。

### Step 3：安装 daemon

```bash
cd /path/to/clawd-mochi/clawd-daemon
pip install -e ".[dev]"

# 验证
clawd-daemon --version
# 应该输出: 0.1.0

# 跑测试
pytest
# 应该看到: 13 passed
```

### Step 4：配置 ESP32 地址

```bash
# 用第 7 节记住的 IP 地址替换
clawd-daemon install --esp32-host 192.168.1.123
```

这个命令做了几件事：

1. 写默认配置到 `~/.config/clawd-daemon/config.toml`
2. 注册 launchd（macOS）或 systemd（Linux）服务，开机自启
3. 启动 daemon 后台运行

> 💡 Mac 上如果看到 `Unload failed: 5: Input/output error`，这是正常的、可以忽略——它只是试图卸载一个还不存在的旧服务。**实际新服务已经加载成功了**，见下面的验证方法。

#### 验证 daemon 在运行

```bash
# 方法 1：检查进程
launchctl list | grep clawd
# 如果输出一行包含 PID 的数字，说明在跑

# 方法 2：健康检查
curl http://127.0.0.1:7878/health
# 应该返回 {"ok":true}

# 方法 3：看日志
cat ~/Library/Logs/clawd-daemon.log | tail -10
# 应该看到 "daemon ready on :7878"
```

### Step 5：装 Claude Code hooks

```bash
clawd-daemon install-hooks
```

这会在 `~/.claude/settings.json` 加上 5 个 hook（PreToolUse / PostToolUse / Stop / Notification / UserPromptSubmit），每次 Claude 触发都会通过 curl 推一条消息给 daemon。

### Step 6：测试推送

> ⚠️ **测试前必须确认两件事**：
> 1. **电脑和 ESP32 连同一个 WiFi**（你家路由器下的同一个子网）
> 2. **ESP32 屏幕显示了 STA IP**（不是只显示 `setup STA at /wifi`）
>
> 先 ping 一下确认能通：
> ```bash
> ping -c 2 192.168.1.123   # 换成你 ESP32 的实际 IP
> ```
> 如果 ping 不通，看[第十一节 WiFi 问题](#wifi-问题companion-版)。

```bash
clawd-daemon test-push --state thinking
# 屏幕应该立即变蓝、自动切到 Status 视图、眼睛开始扫视

clawd-daemon test-push --state done
# 屏幕变绿，眼睛变成 > <

clawd-daemon test-push --state error
# 屏幕变红，眼睛变成 X
```

每条命令打到 ESP32 后屏幕都该立即响应。如果没反应，看[第十一节](#十一常见问题)。

#### 改 ESP32 IP（家里 WiFi 重新分了地址 / 之前写错了）

直接编辑配置文件 → 重启 daemon：

```bash
# 编辑配置
vi ~/.config/clawd-daemon/config.toml
# 改 esp32_host = "新IP"

# macOS 重启 daemon
launchctl unload ~/Library/LaunchAgents/com.clawd.mochi-daemon.plist
launchctl load   ~/Library/LaunchAgents/com.clawd.mochi-daemon.plist

# Linux 重启 daemon
systemctl --user restart clawd-daemon
```

---

## 九、组装外壳

```
1. 把屏幕用 2 颗 M2×6mm 螺丝固定到外壳前盖的窗口位
2. ESP32 用双面胶贴在背板内侧（USB-C 口对准背板的开槽）
3. 理好 8 根线，塞进外壳内部空腔（线太长可以盘起来）
4. USB-C 线从背板的开槽穿出
5. 合上背板（一般是卡扣式或螺丝固定）
```

> 💡 **小技巧**：先别合背板，先做完第十节的功能测试，确认一切正常再封壳。

---

## 十、验证与使用

### 原版用户

通电后：

1. 手机连 `ClaWD-Mochi` 热点
2. 打开 http://192.168.4.1
3. 试每一个按钮：眼睛、画板、终端 → 屏幕应有对应变化

### Companion 版用户

通电后：

1. **第一关：屏幕显示启动画面 + Logo + WiFi 信息** ✅
2. **第二关：屏幕显示 STA: 你家WiFi 的 IP** ✅ → 说明 WiFi 配好了
3. **第三关：跑 `clawd-daemon test-push --state thinking`** ✅ → 屏幕变蓝
4. **第四关：开个 Claude Code 会话随便聊几句**：
   - 屏幕顶部出现 `git:分支名 · 项目名`
   - Claude 调工具时屏幕变蓝，显示 `> Edit` `> Bash` 等
   - 屏幕底部 token 进度条增长
   - Claude 回答完后屏幕变绿（眼睛变 `> <`）几秒后回到橙色

如果每一关都通过，恭喜你 🎉，**完整体验已就绪**！

### Status 视图布局参考

```
┌─────────────────────────────┐
│ git:main · clawd-mochi   📶 │  ← 顶栏：git 分支 + WiFi
├─────────────────────────────┤
│                             │
│     ●●         ●●           │  ← 眼睛（状态相关动画）
│                             │
│  > Edit  3s                 │  ← 当前工具 + 耗时
│  - 实现登录功能              │  ← 当前 todo 任务
│                             │
│ ┌───────────────────────┐   │
│ │█████░░░░░░░░░░░░░░░░░│   │  ← token 进度条
│ └───────────────────────┘   │
│ 45k / 200k tokens   12 tools│
└─────────────────────────────┘
背景色随状态变化（心情灯）：
  橙=空闲  蓝=思考  黄=待确认  绿=完成  红=出错
```

### Web 控制器（手机/电脑都能用）

浏览器打开 ESP32 IP（家里 WiFi 的那个 IP，**不是 192.168.4.1**），有完整 GUI 控制：

- 切换 5 种视图
- 调节动画速度
- 改背景色 / 画笔色
- 开关背光
- 画板涂鸦
- 终端输入

---

## 十一、常见问题

### 硬件问题

<details>
<summary><b>屏幕完全不亮</b></summary>

1. 检查 VCC 是不是接 3.3V（不是 5V）
2. 检查 GND 是不是接好
3. 用万用表量 ESP32 的 3V3 引脚是不是有电压
4. 试着按一下 ESP32 上的 RST 重启
</details>

<details>
<summary><b>屏幕白屏</b></summary>

SDA 和 SCL 接反了，或者 CS / DC 接错了。仔细对照接线表。
</details>

<details>
<summary><b>屏幕花屏 / 偶尔抖动</b></summary>

通常是杜邦线接触不良。把 8 根线一根根重新插紧。
</details>

### 软件问题

<details>
<summary><b>Arduino IDE 找不到 ESP32 端口</b></summary>

- 换数据线（不是充电线）
- 换 USB 口
- Mac 不需要装驱动；少数旧设备可能需要 CH340 驱动
- 长按 BOOT 键再插 USB
</details>

<details>
<summary><b>编译报错 ArduinoJson.h not found</b></summary>

回到 Step 3 装 ArduinoJson 库。
</details>

<details>
<summary><b>上传失败 Failed to connect</b></summary>

按住板子上 BOOT 键 → 点 Upload → 看到 "Connecting..." 后松开。
</details>

### WiFi 问题（Companion 版）

<details>
<summary><b>ESP32 连不上家里 WiFi</b></summary>

- 确认 WiFi 是 2.4GHz（ESP32-C3 不支持 5GHz）
- 密码里别有特殊字符或空格
- 信号强度足够（屏幕上 -85 dBm 以下基本连不上）
- 路由器没开 MAC 地址过滤
</details>

<details>
<summary><b>STA 配置丢失 / 重启后连不上</b></summary>

- 进入 ESP32 的 `/wifi` 页重新添加
- 配置存在 NVS 里，烧录新固件不会清空，但 `esptool.py erase_flash` 会
</details>

### Daemon 问题

<details>
<summary><b>conda activate clawd 报 'Run conda init before conda activate'</b></summary>

```bash
conda init zsh   # 或 bash
```
然后关掉终端开新的。
</details>

<details>
<summary><b>pip install 提示 setup.py not found</b></summary>

用的是系统老 pip（21.x）。激活 conda 环境后用环境里的 pip：
```bash
conda activate clawd
pip install -e ".[dev]"
```
</details>

<details>
<summary><b>test-push 超时</b></summary>

- ESP32 IP 是否正确？看屏幕显示的 STA IP，再对比 `~/.config/clawd-daemon/config.toml` 里写的
- 电脑和 ESP32 是不是连同一个 WiFi？
- 试 `ping <ESP32_IP>` 看通不通
- ESP32 是不是只开着 AP，没连家里 WiFi？看屏幕有没有显示 `STA: ...` 那一行
</details>

<details>
<summary><b>launchctl load 报 Input/output error</b></summary>

这个错误信息**很有误导性**——多数情况下服务实际已经加载成功了。验证一下：

```bash
launchctl list | grep clawd
# 看到一行有 PID 就是在跑

curl http://127.0.0.1:7878/health
# 返回 {"ok":true} 说明 daemon 正常
```

`clawd-daemon install` 命令里那条"Unload failed"也是同样原理——只是试图卸旧服务但没找到，可以忽略。
</details>

<details>
<summary><b>装好 daemon 但 launchctl 找不到</b></summary>

```bash
# 检查 plist 是否存在
ls -la ~/Library/LaunchAgents/com.clawd.mochi-daemon.plist

# 不在？重新跑 install
clawd-daemon install --esp32-host <你的IP>
```
</details>

<details>
<summary><b>conda 环境内 clawd-daemon 命令找不到</b></summary>

```bash
# 确认环境激活了（提示符里有 (clawd)）
conda activate clawd

# 再确认 daemon 装到这个环境了
pip list | grep clawd
which clawd-daemon
# 应该输出 /opt/homebrew/Caskroom/miniconda/base/envs/clawd/bin/clawd-daemon

# 没装？重新装一次
cd ~/yuu-project/clawd-mochi/clawd-daemon
pip install -e .
```
</details>

<details>
<summary><b>Claude Code 跑了但屏幕没反应</b></summary>

1. 确认 daemon 在跑：
   ```bash
   curl http://127.0.0.1:7878/health
   # 应该返回 {"ok":true}
   ```
2. 确认 hooks 装好了：
   ```bash
   cat ~/.claude/settings.json | grep 7878
   ```
3. 看 daemon 日志：
   - macOS: `tail -f ~/Library/Logs/clawd-daemon.log`
   - Linux: `journalctl --user -u clawd-daemon -f`
4. 在屏幕上看 "daemon offline" 字样了？说明 60 秒没收到心跳 → daemon 死了或推不动
</details>

<details>
<summary><b>屏幕显示 "daemon offline"</b></summary>

ESP32 60 秒没收到推送：
- daemon 进程没在跑？`launchctl list | grep clawd` (Mac) / `systemctl --user status clawd-daemon` (Linux)
- ESP32 断网了？看屏幕右上角 WiFi 图标
</details>

### 服务管理

<details>
<summary><b>怎么停 / 重启 daemon？</b></summary>

**macOS：**
```bash
launchctl unload ~/Library/LaunchAgents/com.clawd.mochi-daemon.plist  # 停
launchctl load   ~/Library/LaunchAgents/com.clawd.mochi-daemon.plist  # 启
```

**Linux：**
```bash
systemctl --user stop    clawd-daemon
systemctl --user restart clawd-daemon
systemctl --user status  clawd-daemon
```

**完全卸载：**
```bash
clawd-daemon uninstall
```
</details>

---

## 十二、二次开发

### 改眼睛大小

编辑 `clawd_mochi_companion/clawd_mochi_companion.ino` 顶部：

```cpp
#define EYE_W   30    // 眼睛宽度
#define EYE_H   60    // 眼睛高度
#define EYE_GAP 120   // 两眼间距
```

改完重新 Upload。

### 改心情灯颜色

找到 `initColours()` 函数：

```cpp
MOOD_THINKING  = tft.color565(0,   80,  160);    // 蓝色
MOOD_AWAITING  = tft.color565(200, 160, 0);      // 黄色
// ...
```

数值是 RGB（0-255）。

### 加新动画

照着 `drawNormalEyes()` 或 `drawStatusEyes()` 的模式写一个新的 draw 函数，然后在视图切换的 switch 里加一项。

### 改 Daemon 行为

`clawd-daemon/clawd_daemon/server.py` 里的 `Daemon` 类有 5 个 hook handler，可以加自己的逻辑（比如解析特定工具的输出做特殊显示）。

### OTA 更新（让玩偶不用拆壳就能升级）

参考 `ArduinoOTA` 库，几十行代码可以加上无线烧录。

### 接 MQTT / Home Assistant

ESP32 上加 `PubSubClient` 库，把状态同时推到 MQTT broker，就能在 Home Assistant 里订阅了。

---

## 📂 仓库结构速查

```
clawd-mochi/
├── README.md                              ← 项目首页
├── 操作指南.md                            ← ⭐ 本文档
├── LICENSE                                ← MIT
│
├── clawd_mochi/                           ← 原版固件
│   └── clawd_mochi.ino
│
├── clawd_mochi_companion/                 ← ⭐ Companion 固件
│   └── clawd_mochi_companion.ino
│
├── clawd-daemon/                          ← Python daemon
│   ├── README.md
│   ├── pyproject.toml
│   ├── clawd_daemon/                      ← 源码
│   ├── hooks_config/settings.json         ← Claude hooks 模板
│   └── tests/                             ← 13 个单元测试
│
├── models/                                ← 3D 打印模型
│   ├── clawd_mochi/                       ← 带屏幕外壳
│   ├── clawd_3d/                          ← 装饰摆件
│   └── clawd_3d_squished_eyes/            ← 摆件眯眼版
│
├── docs/superpowers/specs/                ← 设计文档
│   └── 2026-06-06-claude-companion-design.md
│
└── pics/                                  ← 图片素材
```

---

## 🆘 求助渠道

| 问题类型 | 求助方式 |
|----------|----------|
| 硬件 | 阅读 [第十一节](#十一常见问题) 硬件部分 |
| 软件 / 烧录 | 阅读 [第十一节](#十一常见问题) 软件部分 |
| Daemon | `clawd-daemon/README.md` 的 Troubleshooting |
| 提交 issue | GitHub Issues（这个仓库） |
| 想贡献 | Fork → PR |

---

## 🦀 完成了吗？

如果你跟着走完了所有步骤，现在你的桌面上应该有一只：

- 🟠 **空闲时**：橙色背景，慢悠悠眨眼
- 🔵 **Claude 思考时**：蓝色背景，眼睛扫视
- 🟡 **等待你确认时**：黄色闪烁
- 🟢 **任务完成时**：绿色背景，眼睛变成笑脸 `> <`
- 🔴 **出错时**：红色背景，眼睛变成 `X X`

——一只能跟你一起写代码的小螃蟹 🦀

**Enjoy hacking with Claude!**
